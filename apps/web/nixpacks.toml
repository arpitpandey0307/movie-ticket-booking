[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["NEXT_PUBLIC_API_URL=https://movie-bookingapi-production.up.railway.app npm run build"]

[start]
cmd = "npm run start"
